export enum ACTIONS {
  perform = 'PER', // execute: 'value' must be a function
  create = 'CRE', // insert a new element
  delete = 'DEL', // remove an element
  set = 'SET', // change an elements property
  update = 'UPDATE', // change the value of a property
}
